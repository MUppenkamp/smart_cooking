import { TPatchShoppingListItemsParams, TPatchShoppingListItemsReturns } from "../../types/patchShoppingList";

export const patchShoppingListItems = async (params: TPatchShoppingListItemsParams): Promise<TPatchShoppingListItemsReturns | null> => {
    return null;
};
